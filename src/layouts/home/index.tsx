import * as React from 'react'
import { Topbar } from '../../components/topbar'

export const Home = () => {
    return (
        <div className="home-container">
            <Topbar />
        </div>
    )
}
